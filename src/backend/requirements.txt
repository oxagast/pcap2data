PyInstaller==6.21.0
# staticx re-wraps the PyInstaller onefile output so the bundled
# Python interpreter links its loader dependencies statically and can
# be run on other Linux machines without matching libpython / libstdc++
# / etc. versions. Linux-only; macOS/Windows targets ignore it.
#
# 0.13.0 has a known incompatibility with PyInstaller 5.10+ (it reads
# ``pyi_ar.toc.data`` which no longer exists; the build dies with
# ``AttributeError: 'dict' object has no attribute 'data'``).
# 0.14.x added a ``CArchiveReaderPre510Adapter`` to bridge the new
# archive format. Pin to 0.14.x.
staticx==0.14.2 ; sys_platform == "linux"
beautifulsoup4==4.14.3
geoip2==2.9.0
numpy==2.5.2
PyYAML==6.0.3
Requests==2.32.5
scapy==2.5.0
scipy==1.17.1
setuptools==68.1.2
typing_extensions==4.15.0
chardet==7.4.3
libmagic==1.0 ; sys_platform != "win32"
python_magic==0.4.27 ; sys_platform != "win32"
python-magic-bin==0.4.14 ; sys_platform == "win32"
cryptography==49.0.0
