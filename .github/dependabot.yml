# .github/dependabot.yml
version: 2
updates:
  # Node.js / npm packages (Electron renderer + webpack build tooling)
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    open-pull-request-limit: 10
    labels:
      - dependencies
      - node
    commit-message:
      prefix: "deps(npm)"

  # Python packages — both requirements.txt and requirements-extract.txt
  # Dependabot will open separate PRs per manifest automatically
  - package-ecosystem: pip
    directory: /src/backend
    schedule:
      interval: weekly
    open-pull-request-limit: 10
    labels:
      - dependencies
      - python
    commit-message:
      prefix: "deps(pip)"
    ignore:
      - dependency-name: "*"
        update-types: ["version-update:semver-major"]
